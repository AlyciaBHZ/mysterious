# 自定义域名（精简版）

这个文档已被收敛到 `DEPLOY.md`，避免在半开源仓库里堆过多与供应商/账户相关的操作细节。

- **当前域名**：`mysterious.lexaverse.dev`
- **核心要点**：
  - 仓库里保留 `CNAME`（或 `public/CNAME`）写入域名
  - GitHub Settings → Pages 设置 Custom domain 并开启 HTTPS
  - DNS 配置按 GitHub Pages 官方指引做即可

详见：`DEPLOY.md`





